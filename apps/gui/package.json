{
  "name": "phone-backup-gui",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix ui",
    "build": "npm run build --prefix ui",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.0"
  }
}
